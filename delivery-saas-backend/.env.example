# Example environment for delivery-saas-backend
# Copy this file to `.env` and fill in real secrets locally / in your deploy

# Database (example formats)
# For Postgres: postgres://user:pass@host:5432/dbname
DATABASE_URL=postgres://app:changeme@db:5432/appdb

# Server
PORT=3000
JWT_SECRET=change-me-please
PUBLIC_FRONTEND_URL=http://localhost:5173
FRONTEND_ORIGIN=http://localhost:5173
DISABLE_SSL=1

# iFood credentials (fill with your app credentials)
IFOOD_CLIENT_ID=your-ifood-client-id
IFOOD_CLIENT_SECRET=your-ifood-client-secret
IFOOD_MERCHANT_ID=

# Optional: Evolution / Chat API
EVOLUTION_API_BASE_URL=
EVOLUTION_API_API_KEY=

# Optional: OpenAI (if used)
OPENAI_API_KEY=
USE_AI_PARSER=false

# Printer settings
PRINTER_INTERFACE=
PRINTER_TYPE=EPSON
PRINTER_WIDTH=48

# Certificate encryption key (base64, at least 32 bytes decoded)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Required for storing PFX certificate passwords securely.
CERT_STORE_KEY=

# Node env
NODE_ENV=development

# Note: Do NOT commit real secrets. Use CI/CD environment variables or
# platform-specific secret stores (EasyPanel env config, Docker secrets,
# GitHub Actions secrets, etc.) for production values.
