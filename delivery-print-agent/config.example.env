# Backend WebSocket URL (socket.io)
BACKEND_SOCKET_URL=https://your-saas-backend.example.com
# Identifier to authenticate this agent (could be storeId or API token)
# Identifier to authenticate this agent (could be storeId or API token)
STORE_ID=your-store-id-or-token
# Optional: if this agent should serve multiple stores for the same company,
# provide a comma-separated list of store ids. The agent will send this list
# to the backend during Socket.IO auth as `storeIds`.
STORE_IDS=store-1,store-2
# Secret token that the backend will validate on Socket.IO connect
# Set a unique token per agent (required if backend expects token auth)
PRINT_AGENT_TOKEN=replace-with-secure-token
# Optional socket namespace or path (if your backend uses one)
SOCKET_NAMESPACE=/
# Printer interface (examples):
# - Windows: "printer:Your Printer Name"
# - Linux USB: "/dev/usb/lp0"
# - Network: "tcp://192.168.0.50"
PRINTER_INTERFACE=printer:EPSON_TM_T20
# Printer type: EPSON or STAR
PRINTER_TYPE=EPSON
# Log directory
LOG_DIR=./logs
# Optional: run the agent as dry-run (don't actually call the printer)
DRY_RUN=false
