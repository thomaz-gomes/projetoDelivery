# URL do backend (Socket.IO)
BACKEND_SOCKET_URL=https://seu-backend.example.com

# IDs das lojas que este agente atende (separados por virgula)
STORE_IDS=store-id-1,store-id-2

# Token de autenticacao do agente (gerado no painel: Configurar Impressao > Gerar Token)
PRINT_AGENT_TOKEN=

# Nome da impressora no sistema (ex: "EPSON TM-T20II Receipt")
# Deixe vazio para usar a impressora padrao
PRINTER_NAME=

# Tipo da impressora: EPSON ou STAR
PRINTER_TYPE=EPSON

# Largura do papel em colunas (48 para 80mm, 32 para 58mm)
PRINTER_WIDTH=48

# Numero de copias por pedido (1-10)
COPIES=1

# Modo teste: true = nao envia para impressora, apenas loga
DRY_RUN=false
