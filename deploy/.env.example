# =============================================================
# Delivery SaaS - Variáveis de Produção
# =============================================================
# Copie este arquivo para .env e preencha os valores.
# NUNCA commite o .env com secrets reais!
# =============================================================

# --- Domínios (OBRIGATÓRIO - ajuste para seus domínios) ---
API_DOMAIN=api.deliverywl.com.br
APP_DOMAIN=app.deliverywl.com.br

# --- URLs Públicas (OBRIGATÓRIO) ---
VITE_API_URL=https://api.deliverywl.com.br
PUBLIC_FRONTEND_URL=https://app.deliverywl.com.br
FRONTEND_ORIGIN=https://app.deliverywl.com.br

# --- Banco de Dados PostgreSQL (OBRIGATÓRIO) ---
POSTGRES_USER=delivery
POSTGRES_PASSWORD=03t01F2007TF#@
POSTGRES_DB=delivery_prod
# DATABASE_URL: monte a URL completa aqui. Se a senha tiver caracteres
# especiais (#, @, %, etc), faça URL-encode: # → %23, @ → %40
# Exemplo com senha "03t01F2007TF#@" → "03t01F2007TF%23%40"
DATABASE_URL=postgresql://delivery:03t01F2007TF%23%40@delivery_db:5432/delivery_prod

# --- Autenticação (OBRIGATÓRIO) ---
# Gere com: openssl rand -base64 32
JWT_SECRET=RaSxZZj3+0Z9Vdrhda1NZmsggLV+H+6ah0/VQ9pJ+Sc=

# --- iFood (opcional) ---
IFOOD_CLIENT_ID=
IFOOD_CLIENT_SECRET=
IFOOD_MERCHANT_ID=
IFOOD_WEBHOOK_SECRET=
IFOOD_BASE_URL=https://merchant-api.ifood.com.br
IFOOD_AUTH_BASE=https://merchant-api.ifood.com.br/authentication/v1.0
IFOOD_POLL_INTERVAL_MS=30000
IFOOD_POLL_MAX_CONCURRENCY=3

# --- WhatsApp / Evolution API (opcional) ---
EVOLUTION_API_BASE_URL=
EVOLUTION_API_API_KEY=

# --- OpenAI / AI Parser (opcional) ---
OPENAI_API_KEY=
USE_AI_PARSER=false

# --- Email para certificado SSL (OBRIGATÓRIO) ---
SSL_EMAIL=tomgomes2006@gmail.com
