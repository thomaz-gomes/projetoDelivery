# Production frontend environment — baked into build
# VITE_API_URL is overridden by the Docker build-arg in docker-compose.
VITE_API_URL=https://api.deliverywl.com.br
VITE_BACKEND_URL=https://api.deliverywl.com.br
# VITE_SOCKET_URL auto-derives from VITE_API_URL (see config.js)
# Only set explicitly if socket endpoint differs from API URL.
# VITE_SOCKET_URL=
