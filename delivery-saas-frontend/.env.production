# Production frontend environment — baked into build
VITE_API_URL=https://deliverywl.com.br/api
VITE_BACKEND_URL=https://deliverywl.com.br/api
# If your production frontend uses a different socket URL, set it here
VITE_SOCKET_URL=wss://deliverywl.com.br
# Host port used for serving frontend via Docker Compose mapped to host
# When running locally on the VPS we bind frontend to host port 5173
FRONTEND_HOST_PORT=5173
