# Production frontend environment — baked into build
VITE_API_URL=https://delivery-backend.x8bxsh.easypanel.host
VITE_BACKEND_URL=https://delivery-backend.x8bxsh.easypanel.host
# If your production frontend uses a different socket URL, set it here
# VITE_SOCKET_URL=wss://delivery-backend.x8bxsh.easypanel.host
