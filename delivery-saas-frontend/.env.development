VITE_API_URL=http://localhost:3000

# For local dev we recommend leaving VITE_API_URL empty so the frontend uses
# the Vite dev server origin (relative requests) and the configured proxy
# forwards /api, /auth and /socket.io to your backend (http://localhost:3000).
# For production set a full absolute URL, e.g.:
# VITE_API_URL=https://api.yourdomain.com
