# Example environment for delivery-saas-frontend
# Copy these values to `.env.production` (or set at build time) before building

# Absolute backend URL used by the frontend at runtime/build-time
# Use http://<host>:3000 for direct backend access, or leave empty to use same origin
VITE_API_URL=http://localhost:3000

# Socket endpoint (optional). If empty, socket client will connect to same origin.
VITE_SOCKET_URL=
VITE_ENABLE_QZ=1
# Disable SSL when backend uses HTTP in dev environments
# Set to 1 to prefer http://localhost:3000 behaviour in dev
DISABLE_SSL=1

# When building via Dockerfile, pass VITE_API_URL as build-arg:
# docker build --build-arg VITE_API_URL="$VITE_API_URL" -t frontend:prod .
